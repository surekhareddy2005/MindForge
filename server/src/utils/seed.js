export const seedUsers = async () => {
  console.log("seedUsers: seeding is disabled.");
  return [];
};

export const seedCourses = async (users = []) => {
  console.log("seedCourses: seeding is disabled.");
};